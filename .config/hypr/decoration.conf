decoration {
    rounding = $ROUNDING
    rounding_power = 2
    active_opacity = $ACTIVE_OPACITY
    inactive_opacity = $INACTIVE_OPACITY

    shadow {
        enabled = false
    }

    blur {
        enabled = true
        size = $BLUR_SIZE
        passes = 1

        vibrancy = 0.1696
    }
}
